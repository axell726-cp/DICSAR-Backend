package com.dicsar.entity;

public enum Rol {
    ADMIN,      // Acceso total al sistema, reportes y exportaciones
    VENDEDOR    // Solo puede agregar productos, movimientos (entradas/salidas), sin exportaciones
}
